# 本地迁移验证记录

此记录只说明本地验证范围，不表示已通过千岛真机验收或审核。

## 已执行

- 原游戏 vinext 构建成功；`node --test tests/rendered-html.test.mjs`：63/63。
- `npm run test:miniapp`：4/4，覆盖模式识别、存储拒绝/配额异常、媒体生命周期及既有阅读门槛接线。
- `pnpm --dir miniapp/frontend test`：5/5，覆盖 URL 安全约束、加载失败、超时、重试旧回调隔离、隐藏/显示和卸载。
- `npm run lint`：0 error，原有 `PROTAGONIST_NAME` 未使用警告保留。
- `QDMP_GAME_URL=https://starwave0225.github.io/ARG_1304/` 下网页静态导出成功，根页与 `/truth/` 均生成；此操作没有部署网页。
- Taro 4.2.0 编译成功；`qdmp build` EMP 本地打包成功，产物位于 `frontend/dist/main/`，未上传。
- 375×812 独立 Edge 无头浏览器冒烟：干净开场 → 工牌登录 → 工作台 → 刷新恢复；无水平溢出；平台参数在 hash 跳转中保留。
- 同一浏览器以本地测试存档恢复最后一篇日记，实际等待阅读门槛，再走“无画面校验”；千岛模式没有摄像头按钮，后续状态正常推进。
- 浏览器主动禁用 localStorage：能看到告警，仍能进入工作台，没有未捕获页面异常。
- 原生页面独立代码审查及聚焦复审通过。状态层固定定位、WebView 位于页面起点，避免加载说明把原生层推出首屏。

## 构建告警与边界

- 本机 Node 22.12.0 低于仓库声明的 >=22.13.0；上述本机构建通过，但 README/CI 使用符合要求的 Node 22。
- EMP 提示 Taro 运行库存在 `wx.cloud` 不支持 API 与 `/comp` 循环依赖；本应用源码没有调用 cloud，编译完成。保留记录，不能据此推断所有运行库 API 都受支持。
- 本地浏览器只验证网页，不包含千岛原生导航、WebView 叠层、宿主域名策略或桥接事件。
- 容器的编译检查目标是现有公开主站；只有另行托管本分支网页产物后，容器才能使用本次网页适配。
- 平台允许的承载形态、恐怖内容审核、Android/iOS 全流程与前后台测试仍未完成，见 `README.md` 验收清单。
